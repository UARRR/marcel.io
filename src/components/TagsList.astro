---
interface Props {
    tags: [string],
}

const { tags } = Astro.props;
---

<ul class="tagsList">
    {tags.map((tag: string) => 
        <span class="tag">{tag}</span>
    )}
</ul>


<style>
    .tagsList {
        display: flex;
        gap: 0.5em;
    }

    .tag {
        font-size: 0.8rem;
        padding: 2px 8px;
        color: var(--tx-2);
        background-color: var(--bg-2);
        border-radius: 30px;
    }
</style>